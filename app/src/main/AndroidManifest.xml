<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.diucgpacalculator">

    <application
        android:allowBackup="true"
        android:icon="@drawable/cgpa_app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DIUCGPACalculator">
        <activity android:name=".AddingCourseActivity"></activity>
        <activity android:name=".CourseWiseActivity" />
        <activity android:name=".SemesterActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Adding_Semesters" />
    </application>

</manifest>